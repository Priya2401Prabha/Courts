<?xml version="1.0" ?>
<odoo>
    <record id="product_template_only_form_inherit_internal_ref" model="ir.ui.view">
        <field name="name">product.template.view.form.inherit.internal.ref</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="model">product.template</field>
        <field name="arch" type="xml">
            <field name="default_code" position="after">
                <field name="auto_check" readonly="True" invisible="True"/>
                <field name="sequence_generated" readonly="True" invisible="True"/>
            </field>
            <field name="default_code" position="attributes">
                <attribute name="readonly">auto_check</attribute>
            </field>
        </field>
    </record>
</odoo>